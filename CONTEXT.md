# AetherGenix · Domain Context

## 核心工作流

**以图生图 (Reference-based Generation)**
商家上传商品原图作为参考，AI 围绕该参考重新生成一张同品类的高质量场景图。
不保证产品细节 100% 还原，输出物是 AI 再创作的新图，而非对原图的精确修改。
对应后端 API：`/v1/images/generations`（带 reference images）。

与「AI 改图 (Image Edit)」的区别：改图严格保留原图产品外观，仅替换背景/场景；
以图生图允许 AI 重新诠释产品，适合展示氛围感和场景感而非还原精度。

---

## 竞品参照

**豆绘AI (douhuiai.com)**：任务先行 UI——用户看到具体任务按钮（一键白底图 / 一键场景图 / 一键卖点图），每个任务有独立专属流程。采用按图计费（豆点）而非订阅制。

---

## 产品核心术语

| 术语 | 定义 |
|---|---|
| **以图生图** | 商家上传参考图 → AI 重新生成同品类场景图（非精确编辑） |
| **AI 改图** | 直接修改原图（背景替换等），保留产品细节（对应 `/v1/images/edits`） |
| **商品图** | 商家已有的原始产品照片，作为参考输入 |
| **场景图** | AI 生成的最终输出图，包含特定背景/氛围 |
| **风格模板** | 预设的 style + scenarios 组合，供商家一键选择（白底/渐变/室内/户外/海报/节日） |

---

## 页面与路由术语

| 术语 | 路由 | 定义 |
|---|---|---|
| **创作页** | `/create` | 已登录用户默认落地页。输入提示词、选模式、发起生成任务。 |
| **探索页** | `/explore` | 未登录用户默认落地页。公开灵感图库，纯浏览，无过滤。 |
| **操作台** | `/workspace/:taskId` | 单次任务完成后的专属工作空间。含结果展示、再生成、图片编辑、AI 营销文案。有独立 URL，可书签。 |
| **任务** | — | 一次图像生成请求的完整生命周期单元（提交→完成/失败），对应一个操作台。 |
| **创作模式** | — | 创作页内的工作流分支，用扇形叠层卡片切换。目前有「通用生成」和「电商生成」。 |
| **灵感图库** | — | 探索页展示的公开 AI 生成图片集合。 |
| **复用提示词** | — | 探索页图片卡片的核心悬停操作。将该图 prompt 带入创作页输入框，是探索→创作的主转化路径。 |
| **余额** | — | 用户账户内的消费额度，常驻显示在顶部导航栏右侧。 |
